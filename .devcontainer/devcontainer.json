{
    "name": "AutoGenius Dev Container",
    "build": {
      "dockerfile": "Dockerfile"
    },
    "forwardPorts": [
      3000,
      8000
    ],
    "postCreateCommand": "cd frontend && npm install && cd ../backend && xargs -a requirements.txt -n 1 pipx install"
  }
  
  